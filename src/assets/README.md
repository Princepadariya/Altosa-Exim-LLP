# Assets

Local images, logos and downloadable files go here — imported from components
so Vite fingerprints and optimises them.

Files that must keep a stable public URL (favicon, OG image, sitemap, robots)
live in `/public` instead.
